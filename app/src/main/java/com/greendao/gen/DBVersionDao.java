package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.actor.cnpc_qhse_exams.bean.DBVersion;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DB_Version".
*/
public class DBVersionDao extends AbstractDao<DBVersion, Long> {

    public static final String TABLENAME = "DB_Version";

    /**
     * Properties of entity DBVersion.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property VersionName = new Property(1, String.class, "versionName", false, "VERSION_NAME");
        public final static Property VersionCode = new Property(2, int.class, "versionCode", false, "VERSION_CODE");
        public final static Property UpdateDoc = new Property(3, String.class, "updateDoc", false, "UPDATE_DOC");
    }


    public DBVersionDao(DaoConfig config) {
        super(config);
    }
    
    public DBVersionDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DB_Version\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"VERSION_NAME\" TEXT NOT NULL UNIQUE ," + // 1: versionName
                "\"VERSION_CODE\" INTEGER NOT NULL UNIQUE ," + // 2: versionCode
                "\"UPDATE_DOC\" TEXT NOT NULL );"); // 3: updateDoc
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DB_Version\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DBVersion entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getVersionName());
        stmt.bindLong(3, entity.getVersionCode());
        stmt.bindString(4, entity.getUpdateDoc());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DBVersion entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getVersionName());
        stmt.bindLong(3, entity.getVersionCode());
        stmt.bindString(4, entity.getUpdateDoc());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public DBVersion readEntity(Cursor cursor, int offset) {
        DBVersion entity = new DBVersion();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DBVersion entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setVersionName(cursor.getString(offset + 1));
        entity.setVersionCode(cursor.getInt(offset + 2));
        entity.setUpdateDoc(cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DBVersion entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DBVersion entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DBVersion entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
